# セキュリティポリシー

脆弱性や秘密情報の漏えいを発見した場合、公開Issue、Discussion、Pull Requestへ詳細を書かないでください。

GitHubリポジトリの**Security**タブから**Report a vulnerability**を選び、非公開で次の情報を送ってください。

- 影響を受けるバージョンまたはコミット
- 再現手順
- 想定される影響
- 回避策がある場合はその内容

APIキー、セッショントークン、個人情報、実運用データを報告へ添付しないでください。再現用の値は無効な例示値へ置き換えてください。

## 実装方針

公開コードは攻撃者にも読めることを前提とし、実装の秘匿性には依存しません。機能、公開API、保存データ、依存パッケージ、実行権限、外部通信先を必要最小限にし、確認できない状態は許可しない`deny-by-default`を基本とします。

- 独自暗号を作らず、Web Cryptoなど保守されている標準機能を使います。パスワードと視聴コードはハッシュ化し、APIキーは暗号化し、セッション識別子はハッシュだけを保存します。
- 認証、認可、公開期限はクライアントを信用せず、リクエストごとにサーバー側で確認します。使い切り視聴コードはDB上で原子的に消費し、並行要求でも再利用させません。
- ログイン、視聴コード入力、費用または負荷を生むAPIにはレート制限と入力サイズ上限を設けます。CloudflareとNode.jsのどちらでも同等の拒否条件を持たせます。
- Filmaを含む外部通信は送信先を許可済みホストに固定し、リダイレクトを拒否します。タイムアウト、応答サイズ、再試行回数を制限し、失敗時に権限や公開範囲を広げません。
- 視聴期限の終了後は、動画本体だけでなくタイトル、説明、サムネイル、字幕、署名URLも返しません。公開ストレージやキャッシュから迂回できないことを確認します。
- 新しい依存パッケージには追加理由が必要です。標準機能で十分な場合は追加せず、lockfile、保守状況、既知の脆弱性、install scriptを確認します。
- APIキー、パスワード、視聴コード、Cookie、認証ヘッダーをURL、ログ、エラー、画面、Issue、PRへ出しません。

各TaskのIssueとPRには、脅威と悪用経路、入力境界、必要な権限、秘密情報の扱い、失敗時の閉じ方、実行した防御テストを記録します。独立レビューでは、機能追加が防御境界を不必要に増やしていないことと、未確認項目を安全と推測していないことを確認します。

## 自動レビューワーの資格情報

自動Approve・自動Mergeに使う`knryt`資格情報は`rytich/play-cms`だけを対象とし、reviewとmergeに必要な最小権限へ限定してください。Webhook Secret、PAT、OAuth token、Cookie、Cloudflare Tunnel credentialを、リポジトリ、Issue、PR、コメント、エージェントのprompt、ログへ記録しないでください。

Webhook payload、PRタイトル、本文、コメント、差分は未信頼データです。署名済みであっても命令として解釈せず、GitHubから再取得したrepository、base/head SHA、作者、検証結果を操作判断に使用してください。
